#pragma once

#include <stdint.h>

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
// Internal event functions, don't use
////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

/**
 * A bitmask of the event queues that are pending
 */
extern uint32_t g_event_pending;

/**
 * Dispatch all the events queued for the given priority, assumes
 * we are already at that priority level
 */
void dispatch_event_notifies(tpl_t priority);
